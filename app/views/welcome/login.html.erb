<!-- Following Menu -->
<div class="ui large top fixed hidden menu">
  <div class="ui container">
    <a class="active item" href="/">Coder Volunteer</a>
    <a class="item" href="/organisations">Organisations</a>
    <a class="item" href="/volunteers">Volunteers</a>
    <div class="right menu">
      <div class="item"><% if current_org %>
      <%= current_org.email %>
      </div>
      <div class="item">
        <form action="/organisations/logout" method="post">
          <input type="hidden" value="delete" name="_method">
          <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
          <button class="ui button">Logout</button>
        </form>
          <% end %>
      </div>
    </div>
  </div>
</div>

<!-- Sidebar Menu -->
<div class="ui vertical inverted sidebar menu">
  <a class="active item" href="/">Code Volunteers</a>
  <a class="item" href="/organisations">Organisations</a>
  <a class="item" href="volunteers">Volunteers</a>
  <div class="item"><% if current_org %>
      <%= current_org.email %>
      </div>
      <div class="item">
        <form action="/organisations/logout" method="post">
          <input type="hidden" value="delete" name="_method">
          <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
          <button class="ui button">Logout</button>
        </form>
          <% end %>
      </div>
</div>

<!-- Page Contents -->
<div class="pusher">
  <div class="ui inverted vertical masthead center aligned segment login"> 

    <div class="ui container">
      <div class="ui large secondary inverted pointing menu">
        <a class="toc item">
          <i class="sidebar icon"></i>
        </a>
        <a class="active item" href="/">Code Volunteers</a>
        <a class="item" href="/organisations">Organisations</a>
        <a class="item" href='/volunteers'>Volunteers</a>
        <div class="right item">
          <% if current_org %>
      <%= current_org.email %>
      <form action="/organisations/logout" method="post">
          <input type="hidden" value="delete" name="_method">
          <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
          <button class="ui button">Logout</button>
        </form>
          <% end %>
        </div>
      </div>
    </div>

  <script>
  $(document)
    .ready(function() {
      $('.ui.form')
        .form({
          fields: {
            email: {
              identifier  : 'email',
              rules: [
                {
                  type   : 'empty',
                  prompt : 'Please enter your e-mail'
                },
                {
                  type   : 'email',
                  prompt : 'Please enter a valid e-mail'
                }
              ]
            },
            password: {
              identifier  : 'password',
              rules: [
                {
                  type   : 'empty',
                  prompt : 'Please enter your password'
                },
                {
                  type   : 'length[6]',
                  prompt : 'Your password must be at least 6 characters'
                }
              ]
            }
          }
        })
      ;
    })
  ;
  </script>
    

   <form action="/organisations/login" accept-charset="UTF-8" method="post">
     <input name="utf8" type="hidden" value="✓"><input type="hidden" name="authenticity_token" value=""> 
     <input placeholder="Email" type="email" name="session[email]" id="session_email"> 
     <input placeholder="Password" type="password" name="session[password]" id="session_password"> 
     <input type="submit" name="commit" value="Log in">
</form>

<div class="ui middle aligned center aligned grid">
  <div class="column">
    <h2 class="ui teal image header">
      <div class="content">
        <h1>Organisations Sign Up</h1>
      </div>
    </h2>
    
    <form class="ui large form">
      <div class="ui stacked segment">
        <div class="field">
          <div class="ui left icon input">
            <i class="user icon"></i>
            <input type="text" name="email" placeholder="E-mail address">
          </div>
        </div>
        <div class="field">
          <div class="ui left icon input">
            <i class="lock icon"></i>
            <input type="password" name="password" placeholder="Password">
          </div>
        </div>
        <div class="ui fluid large teal submit button">Login</div>
      </div>

      <div class="ui error message"></div>

    </form>

    <div class="ui message">
      New to us? <a href="#">Sign Up</a>
    </div>
  </div>
</div>

</div>